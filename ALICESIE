#include<cstdio>
#include<cstdlib>
using namespace std;

int main()
{
	int t;
	scanf("%d",&t);
	for(int i=0;i<t;i++)
	{
		int n;
		scanf("%d",&n);
		if(n%2==0)
			printf("%d\n",n/2);
		else
			printf("%d\n",(n+1)/2);
	}
	return 0;
}
